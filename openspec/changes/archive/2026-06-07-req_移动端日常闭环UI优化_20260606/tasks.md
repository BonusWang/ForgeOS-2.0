## 1. Baseline And Regression Guards / 基线与回归保护

- [x] 1.1 运行现有移动端与同步结构测试，记录当前失败项和已知 V3 同步相关未提交改动，避免把既有脏工作区误判为本次回归。
- [x] 1.2 为同步面板补充结构测试：V3 已初始化时主状态区不得同时展示旧 `基线` 和 `V3 基线`。
- [x] 1.3 为推进页补充结构测试：周日下方存在收纳箱，收纳箱读取 `date === 'BACKLOG'` 的任务，切换周不改变收纳箱来源。
- [x] 1.4 为记录页补充结构测试：灵感流程使用纵向节点，混合记录流被移除，灵感和反思分别有最近列表。
- [x] 1.5 为移动端风格偏好补充结构测试：默认跟随桌面，开启本机独立风格后手机端切换不写入跨端同步风格字段。

## 2. Sync Panel Clarity / 同步面板清晰度

- [x] 2.1 调整 `SyncPanel` 主状态信息：V3 revision 存在时隐藏主状态区旧 `基线` 字段。
- [x] 2.2 如保留旧基线诊断信息，将其移动到次级调试或旧版信息区域，并标明日常同步使用 V3。
- [x] 2.3 确认立即同步、修改配置、配置保存后收起、V3 冲突提示和待上传本地变更展示仍可用。

## 3. Mobile Progress Backlog Inbox / 移动端推进收纳箱

- [x] 3.1 在 `MobileWeekProgress` 中计算跨周收纳任务列表，使用现有 `tasks.filter(task => task.date === 'BACKLOG')`。
- [x] 3.2 在周日节点之后渲染收纳箱区域，包含标题、数量、空态和任务列表。
- [x] 3.3 在收纳箱内实现新增任务，新增任务必须写入 `BACKLOG`，并复用现有任务数据流。
- [x] 3.4 在收纳箱内实现编辑任务描述，空白内容不得覆盖原描述。
- [x] 3.5 在收纳箱内实现删除任务确认态，未确认时不得删除。
- [x] 3.6 在收纳箱内实现安排动作，点击后打开底部安排面板。
- [x] 3.7 安排面板展示当前查看周 7 天快捷按钮，并支持选择其他日期。
- [x] 3.8 选择日期后调用现有任务移动逻辑，将任务移出 `BACKLOG` 并进入目标日期列表。
- [x] 3.9 验证离线本地写入语义不变：收纳箱新增、编辑、删除、安排都能更新本地 store。

## 4. Mobile Task Action UI / 移动端任务操作 UI

- [x] 4.1 将推进页任务操作区从大圆按钮调整为更轻的移动端操作样式。
- [x] 4.2 保留编辑、明天、收纳、删除四个操作和完成/取消完成控件。
- [x] 4.3 保留删除确认态和编辑保存/取消能力。
- [x] 4.4 调整 CSS，确保任务标题、完成控件和操作区在 360px 手机宽度下不重叠。
- [x] 4.5 保留足够触控热区，视觉变轻但操作不可变难点。

## 5. Mobile Capture Page IA / 移动端记录页信息架构

- [x] 5.1 将灵感捕捉横向 stepper 改为纵向递进节点：`01 想法`、`02 来源`、`03 标签`、`04 确认`。
- [x] 5.2 当前节点展开输入或确认内容，已完成节点展示摘要，未开始节点弱化。
- [x] 5.3 保留来源和标签可跳过，保留返回上一步能力。
- [x] 5.4 确认保存仍调用现有 `addInspiration`，并保留来源和自定义标签。
- [x] 5.5 写灵感入口支持展开后收起；非空未保存内容不得静默丢失。
- [x] 5.6 写反思入口支持展开后收起；非空未保存内容不得静默丢失。
- [x] 5.7 取消底部混合「记录流 / 最近保存」区域。
- [x] 5.8 在灵感卡片内部展示最近灵感，包含本地时间、来源和非系统标签。
- [x] 5.9 在反思卡片内部展示最近反思，包含本地时间和反思摘要。
- [x] 5.10 保留反思保存路径：每日反思继续保存到反思库，不得误入灵感库。

## 6. Mobile Local-Only Visual Style / 移动端本机独立风格

- [x] 6.1 明确当前视觉风格状态和同步 config 的关系，选择本机偏好存储位置，确保默认继续跟随桌面。
- [x] 6.2 在手机端系统页风格区域新增本机独立风格开关和说明文案。
- [x] 6.3 开启本机独立风格后，移动端切换风格只更新本机移动端偏好。
- [x] 6.4 关闭本机独立风格后，移动端重新使用跟随桌面的视觉风格。
- [x] 6.5 确认本机独立风格不影响任务、灵感、反思和 V3 业务数据同步。

## 7. Verification / 验证

- [x] 7.1 运行 `node --test tests\\mobileProductStructure.test.ts tests\\syncPanelStructure.test.ts`。
- [x] 7.2 运行与任务、反思、灵感和同步相关的现有测试，确保保存路径和同步状态没有回退。
- [x] 7.3 运行项目构建或类型检查，确认新增 UI 和偏好字段没有类型错误。
- [x] 7.4 使用 360px 到 767px 手机视口检查推进页、记录页和系统页无横向溢出、重叠或不可点击控件。
- [x] 7.5 已检查 Android 设备或模拟器可用性；当前 `adb devices` 无设备，因此未执行安装/启动烟测。
