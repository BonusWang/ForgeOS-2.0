## 1. Requirements Alignment / 需求对齐

- [x] 1.1 更新 OpenSpec 文档，明确新需求是全局风格切换，不是新增 `[◇ 新系统]` 页面跳转。
- [x] 1.2 确认 `Dashboard`、`Reflection`、`System` 继续作为同一套功能组件使用。

## 2. Style Toggle Implementation / 风格切换实现

- [x] 2.1 移除 `systemOrbit` 页面状态和 `[◇ 新系统]` 导航入口。
- [x] 2.2 删除不再使用的独立系统风格页组件。
- [x] 2.3 在 `App.tsx` 中新增本地风格模式状态和 `[◇ 风格切换]` 按钮。
- [x] 2.4 新风格下保持导航入口为周看板、反思库、◇ 系统、[◇ 风格切换]、[⊕ 模块]、[◑]。
- [x] 2.5 使用局部 CSS 变量和外壳类名实现 orbit-general 风格的背景、表面、边框、状态色和交互反馈。

## 3. Functional Preservation / 功能保留

- [x] 3.1 验证风格切换不改变周看板、反思库、系统页的页面状态和数据源。
- [x] 3.2 验证系统页更新检查、数据备份、关于信息和语录仍使用原组件与原逻辑。
- [x] 3.3 验证模块管理和主题切换按钮在两种风格下都仍可使用。

## 4. Verification / 验证

- [x] 4.1 运行静态检查，确认不存在 `systemOrbit`、`SystemOrbit` 或 `[◇ 新系统]` 入口。
- [x] 4.2 运行项目构建。
- [x] 4.3 运行 OpenSpec 校验。
- [x] 4.4 用浏览器验证 `[◇ 风格切换]` 可在两种风格间来回切换，窄屏无明显重叠或裁切。
